import styled from "styled-components";

export const WebBrowserStyle = styled.div`
  & embed {
    position: absolute;
    top: 30px;
    left: 0;
    width: 100%;
    height: calc(100% - 30px);
  }
`;
